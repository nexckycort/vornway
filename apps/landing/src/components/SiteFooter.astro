<footer
  class="relative overflow-hidden py-16"
  style="background: hsl(240 38% 14%); border-top: 1px solid hsl(240 47% 47% / 0.1)"
>
  <div class="mx-auto max-w-6xl px-6">
    <div
      class="flex flex-col gap-12 md:flex-row md:items-start md:justify-between"
    >
      <div class="max-w-xs">
        <div class="flex items-center gap-2">
          <div
            class="flex h-8 w-8 items-center justify-center rounded-lg"
            style="background: hsl(174 73% 48%)"
          >
            <svg
              width="18"
              height="18"
              viewBox="0 0 18 18"
              fill="none"
              aria-hidden="true"
            >
              <path
                d="M9 2L2 9l7 7 7-7-7-7z"
                fill="hsl(240 38% 17%)"
                opacity="0.9"></path>
              <path
                d="M9 6v6M6 9h6"
                stroke="hsl(240 38% 17%)"
                stroke-width="1.5"
                stroke-linecap="round"></path>
            </svg>
          </div>
          <span class="font-display text-lg font-bold" style="color: white"
            >Splitway</span
          >
        </div>
        <p
          class="mt-3 text-sm leading-relaxed"
          style="color: hsl(250 25% 90% / 0.5)"
        >
          Fair splits, fewer awkward conversations.
        </p>
      </div>

      <div class="grid grid-cols-2 gap-8 sm:grid-cols-3">
        {
          [
            {
              title: "Product",
              links: ["Features", "Pricing", "Changelog", "Roadmap"],
            },
            {
              title: "Company",
              links: ["About", "Blog", "Careers", "Press"],
            },
            {
              title: "Legal",
              links: ["Privacy", "Terms", "Cookie Policy"],
            },
          ].map((col) => (
            <div>
              <h4
                class="mb-3 text-xs font-semibold uppercase tracking-widest"
                style="color: hsl(250 25% 90% / 0.3)"
              >
                {col.title}
              </h4>
              <ul class="space-y-2">
                {col.links.map((link) => (
                  <li>
                    <a
                      href={`#${link.toLowerCase()}`}
                      class="text-sm transition-colors duration-200 hover:opacity-100"
                      style="color: hsl(250 25% 90% / 0.6)"
                    >
                      {link}
                    </a>
                  </li>
                ))}
              </ul>
            </div>
          ))
        }
      </div>
    </div>

    <div
      class="mt-12 flex flex-col items-center justify-between gap-4 border-t pt-8 sm:flex-row"
      style="border-color: hsl(240 47% 47% / 0.08)"
    >
      <p class="text-xs" style="color: hsl(250 25% 90% / 0.3)">
        2026 Splitway. All rights reserved.
      </p>
      <div class="flex gap-4">
        {
          ["Twitter", "GitHub", "Discord"].map((social) => (
            <a
              href={`#${social.toLowerCase()}`}
              class="text-xs transition-colors duration-200 hover:opacity-100"
              style="color: hsl(250 25% 90% / 0.4)"
            >
              {social}
            </a>
          ))
        }
      </div>
    </div>
  </div>
</footer>
